{# Index/index.html.twig #}
{% extends layout %}

{% block title %}
{{ parent() }} - Inicio
{% endblock %}

{% block content %}
    {#
    <div class="page_title row">
        <div class="col-md-12">
            <h1>Introduzca título, autor o isbn</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <form name="manto_{{values.controller}}" id="manto_{{values.controller}}" action="" method="POST" enctype="multipart/form-data">
                <input name="controller" value="{{ values.controller }}" type="hidden" />
                <input name="action" id="action" value="buscar" type="hidden" />
                <input id="texto" class="form-control"  name="texto" type="text" value="{{values.texto}}"/>
                <input class="btn btn-primary" type="submit" value="Buscar" id="buscar"/>
            </form>
        </div>
    </div>
    
    {% if values.listado %}
    {% include values.controller ~ '/listado.html.twig' with {'data': values.listado}%}
    {% endif %}

{{ macro.foco('texto') }}
#}

{% endblock %}
