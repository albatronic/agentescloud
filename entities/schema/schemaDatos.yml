# ESQUEMA DE CADA EMPRESA
      
# Almacenes
AgtAlmacenes:
  comment: Almacenes
  columns:   
    Nombre:             { type: string(50), notnull: true, index: index }
    Direccion:          { type: string(50), notnull: true }
    IdPais:             { type: bigint(11), notnull: true, default: 68, index: index, comment: 'emp,Paises,Id' }
    IdProvincia:        { type: bigint(11), notnull: true, default: 0, index: index, comment: 'emp,Provincias,Id' }
    IdPoblacion:        { type: bigint(11), notnull: true, default: 0, index: index, comment: 'emp,Municipios,Id' }
    CodigoPostal:       { type: string(10), notnull: true, default: 0 }
    Telefono:           { type: string(30), notnull: false }
    Fax:                { type: string(30), notnull: false }    
    EMail:              { type: string(50), notnull: false }
    Tipo:               { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,AlmacenesTipos,IDTipo' }
    ControlUbicaciones: { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,ValoresSN,IDTipo' }    
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>    
  relations:
      
# Agencias de transporte
AgtAgencias:
  comment: Agencias de transporte
  columns:   
    Agencia:       { type: string(50), notnull: false, default: NULL }
    Telefono:      { type: string(30), default: NULL }
    Fax:           { type: string(30), default: NULL }
    Web:           { type: string(50), default: NULL }    
    EMail:         { type: string(50), default: NULL }
    CosteEstandar: { type: decimal, notnull: true, default: '0.00' }
    UrlTracking:   { type: varchar(100), default: NULL }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>      
  relations:

# Formas de pago
AgtFormasPago:
  comment: Formas de pago
  columns:
    Descripcion:         { type: string(50), notnull: true }
    NumeroVctos:         { type: int(4), notnull: true, default: 1 }
    DiaPrimerVcto:       { type: int(4), notnull: true, default: 0 } 
    DiasIntervalo:       { type: int(4), notnull: true, default: 0 } 
    CContable:           { type: string(10), notnull: true, default: '0000000000' } 
    AnotarEnCaja:        { type: boolean, notnull: true, default: 0, index: index, comment: 'Abstract,ValoresSN,IDTipo' }
    RecargoFinanciero:   { type: decimal(5), notnull: true, default: '0.00' }
    DescuentoFinanciero: { type: decimal(5), notnull: true, default: '0.00' }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations: 
      
# Bandeja de salida de correos
AgtEmailBox:
  comment: Registo de envíos por email
  columns:
    Ip:        { type: string(20), notnull: true }
    De:        { type: string(100), notnull: true, index: index }
    Para:      { type: text, notnull: true }
    CC:        { type: text, notnull: true, default: '' }
    CCO:       { type: text, notnull: true, default: '' }
    Asunto:    { type: string(100), notnull: true, default: '' }
    Mensaje:   { type: text, notnull: true, defautl: '' }
    Adjuntos:  { type: text, notnull: true, default: '' }
    Ok:        { type: boolean, notnull: true, default: 0}
    Smtp:      { type: text, default: NULL }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>
    
# Articulos
AgtArticulos:
  comment: Artículos
  columns:   
    IdFirma:           { type: bigint(11), notnull: true, index: index, comment: 'datos,Firmas,Id' }      
    Codigo:            { type: string(25), notnull: true, index: index }
    Descripcion:       { type: string(255), notnull: true, index: index }
    Subtitulo:         { type: string(255), default: null }
    Resumen:           { type: string(500), default: null }
    ReclamoCorto:      { type: string(255), default: null }
    ReclamoLargo:      { type: string(500), default: null }    
    IdCategoria:       { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Familias,Id' }
    IdFamilia:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Familias,Id' }
    IdSubfamilia:      { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Familias,Id' }
    Pvd:               { type: decimal(3), notnull: true, default: 0}
    Pvp:               { type: decimal(3), notnull: true, default: 0}
    Margen:            { type: decimal(3), notnull: true, default: 0}
    Pmc:               { type: decimal(3), notnull: true, default: 0}
    IdIva:             { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,TiposIva,Id'}    
    PvpAnterior:       { type: decimal(3), notnull: true, default: 0}
    Etiqueta:          { type: string(30), default: null, index: index }
    CodigoEAN:         { type: string(13), defatul: null }
    Caducidad:         { type: integer(4), notnull: true, default: 0 }
    Garantia:          { type: string(10), default: null }
    Peso:              { type: decimal(), notnull: true, default: 0 }
    Volumen:           { type: decimal(), notnull: true, default: 0 }
    Caracteristicas:   { type: text, defatul: null }
    Referencia1:       { type: string(25), default: null, index: index }    
    Referencia2:       { type: string(25), default: null, index: index }    
    Referencia3:       { type: string(25), default: null, index: index }    
    FechaUltimoPrecio: { type: datetime, defatul: null }
    Vigente:           { type: tinyint(1), notnull: true, default: 1, index: index, comment: 'abstract,ValoresSN,IDTipo' }
    Inventario:        { type: tinyint(1), notnull: true, default: 1, index: index, comment: 'abstract,ValoresSN,IDTipo' }
    Trazabilidad:      { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,ValoresSN,IDTipo' }    
    BajoPedido:        { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,ValoresSN,IDTipo' }
    PackingCompras:    { type: decimal(), notnull: true, default: 1 }
    PackingVentas:     { type: decimal(), notnull: true, default: 1 } 
    Merma:             { type: decimal(), notnull: true, default: 0 } 
    BloqueoStock:      { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,ValoresSN,IDTipo' } 
    IdEstado1:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,ArticulosEstados,Id' }
    IdEstado2:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,ArticulosEstados,Id' }
    IdEstado3:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,ArticulosEstados,Id' }
    IdEstado4:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,ArticulosEstados,Id' }
    IdEstado5:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,ArticulosEstados,Id' }
    StockMinimo:       { type: decimal(), notnull: true, default: 0 } 
    StockMaximo:       { type: decimal(), notnull: true, default: 0 }
    MinimoVentaAlto:   { type: decimal(), notnull: true, default: 0 }
    MinimoVentaAncho:  { type: decimal(), notnull: true, default: 0 }    
    MinimoVenta:       { type: decimal(), notnull: true, default: 0 } 
    MultiploAlto:      { type: decimal(), notnull: true, default: 0 }  
    MultiploAncho:     { type: decimal(), notnull: true, default: 0 } 
    RecargoEnergetico: { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,ValoresSN,IDTipo' }     
    UMB:               { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,UnidadesMedida,Id' }
    UMC:               { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,UnidadesMedida,Id' }
    CUMC:              { type: decimal(), notnull: true, default: 1 }
    UMA:               { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,UnidadesMedida,Id' }    
    CUMA:              { type: decimal(), notnull: true, default: 1 }  
    UMV:               { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,UnidadesMedida,Id' }    
    CUMV:              { type: decimal(), notnull: true, default: 1 } 
    AvisosPedidos:     { type: text, defatul: null }
    AvisosPresupuestos: { type: text, defatul: null }
    AvisosAlbaranes:    { type: text, defatul: null }
    AvisosFacturas:     { type: text, defatul: null }
    UrlProducto:        { type: string(255), default: null }
    UrlImagen:          { type: string(255), default: null }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>    
  relations:
  index:
    Firma_Codigo: { type: unique, columns: 'IdFirma,Codigo'}      
 
# Estados de los artículos
AgtArticulosEstados:
  comment: Estados de los artículos
  columns:   
    Estado:   { type: string(50), notnull: true }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>
  relations:
      
# Unidades de medida de los articulos
AgtUnidadesMedida:
  comment: Unidades de medida de los articulos
  columns:
    UnidadMedida: { type: string(10), notnull: true }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>   
  relations:  
      
# Clientes
AgtClientes:
  comment: Clientes
  columns:   
    RazonSocial:         { type: varchar(50), notnull: true, index: index }
    NombreComercial:     { type: varchar(50), default: null, index: index }
    Cif:                 { type: varchar(15), notnull: true, index: index }
    Direccion:           { type: varchar(50), default: null }
    IdPais:              { type: bigint(11), notnull: true, default: 68, index: index, comment: 'emp,Paises,Id' }
    IdProvincia:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'emp,Provincias,Id' }
    IdPoblacion:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'emp,Municipios,Id' }
    CodigoPostal:        { type: varchar(10), default: null }
    ApdoCorreos:         { type: varchar(10), default: null }
    Telefono:            { type: varchar(30), default: null }
    Fax:                 { type: varchar(30), default: null }    
    Movil:               { type: varchar(30), default: null }    
    EMail:               { type: varchar(50), defatul: null }    
    Web:                 { type: varchar(50), default: null }    
    CContable:           { type: varchar(10), default: null } 
    IdRuta:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Rutas,Id' }    
    IdActividad:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Actividades,Id' }    
    IdGrupoCompras:      { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,GruposCompras,Id' }    
    IdFormaPago:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,FormasPago,Id' }    
    DiaDePago:           { type: int(2), notnull: true, defatul: 0 }
    Iva:                 { type: boolean, notnull: true, default: 0, index: index, comment: 'datos,ValoresSN,IDTipo' }
    RecargoEqu:          { type: boolean, notnull: true, default: 0, index: index, comment: 'datos,ValoresSN,IDTipo' }
    Avisos:              { type: text, default: null }
    Vigente:             { type: boolean, notnull: true, default: 1, index: index, comment: 'datos,ValoresSN,IDTipo' }
    Sexo:                { type: tinyint(1), notnull: true, default: 1, index: index, comment: 'Abstract,Sexos,IDTipo' }
    Tratamiento:         { type: tinyint(1), notnull: true, default: 1, index: index, comment: 'Abstract,Tratamientos,IDTipo' }
    FechaNacimiento:     { type: date, notnull: true, default: '0000-00-00' }
    Login:               { type: varchar(50), default: null, index: index }
    Password:            { type: varchar(50), default: null } 
    Catalogos:           { type: bigint(11), default: null }
    Banco:               { type: varchar(60), default: null }
    DireccionBanco:      { type: varchar(60), default: null }
    Iban:                { type: varchar(34), default: null }
    Bic:                 { type: varchar(11), default: null }
    Mandato:             { type: varchar(35), default: null }
    FechaMandato:        { type: date, notnull: true, default: '0000-00-00' }     
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:     
  
# Clientes Contactos
AgtClientesContactos:
  comment: Contactos de clientes
  columns:   
    IdCliente:  { type: bigint(11), notnull: true, index: index, comment: 'datos,Clientes,Id' }
    Cargo:      { type: varchar(30), notnull: false }
    Nombre:     { type: varchar(50), notnull: true }
    Telefono:   { type: varchar(50), default: null }
    Fax:        { type: varchar(50), default: null } 
    Movil:      { type: varchar(50), default: null }   
    EMail:      { type: varchar(50), default: null } 
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:    
    
# Direcciones de entrega de clientes
AgtClientesDEntrega:
  comment: Direcciones de entrega de clientes
  columns:   
    IdCliente:                { type: bigint(11), notnull: true, index: index, comment: 'datos,Clientes,Id' }
    Nombre:                   { type: varchar(50), notnull: true }    
    Direccion:                { type: varchar(50), notnull: true }
    IdPais:                   { type: bigint(11), notnull: true, default: 68, index: index, comment: 'datos,Paises,Id' }
    IdProvincia:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Provincias,Id' }
    IdPoblacion:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Municipios,Id' }
    CodigoPostal:             { type: varchar(10), default: null }
    Telefono:                 { type: varchar(30), default: null }
    Fax:                      { type: varchar(30), default: null } 
    Movil:                    { type: varchar(30), default: null } 
    EMail:                    { type: varchar(50), default: null }
    PersonaContacto:          { type: varchar(50), default: null }
    EnviarCopiaFactura:       { type: boolean, notnull: true, default: 1, index: index, comment: 'datos,ValoresSN,IDTipo' }    
    FacturacionIndependiente: { type: boolean, notnull: true, default: 0, index: index, comment: 'datos,ValoresSN,IDTipo' }    
    AgenciaHabitual:          { type: bigint(11), default: 0, index: index, comment: 'datos,Agencias,Id' } 
    Horario:                  { type: varchar(50), default: null } 
    HorarioLlamadas:          { type: varchar(50), default: null }
    Vigente:                  { type: tinyint(1), notnull: true, default: 1, index: index, comment: 'Abstract,ValoresSN,IDTipo' }    
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:   
    
# Familias
AgtFamilias:
  comment: Familias
  columns:  
    IdFirma:     { type: bigint(11), notnull: true, index: index, comment: 'datos,Firmas,Id' }
    Descripcion: { type: varchar(100), notnull: true }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:
      
# Grupos de Compras
AgtGruposCompras:
  comment: Grupos de Compras
  columns:   
    Descripcion:   { type: varchar(100), notnull: true }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:
    
# Tipos de Actividades
AgtActividades:
  comment: Tipos de actividades
  columns:    
    Descripcion:   { type: varchar(100), notnull: true }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:  

# Rutas
AgtRutas:
  comment: Rutas comerciales
  columns:   
    Descripcion:    { type: varchar(50), notnull: true, default: ''}
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>      
  relations: 

AgtTarifas:
  comment: Tarifas
  columns:   
    IdArticulo: { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Articulos,Id' }    
    IdTarifa:   { type: bigint(11), notnull: true, default: 0, index: index }    
    Precio:     { type: decimal, notnull: true, default: 0 }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:
  index:
    ArticuloTarifa: { type: unique, columns: 'IdArticulo,IdTarifa' }      

AgtVencimientos:
  comment: Vencimientos
  columns:
    IdFactura: { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,FemitidasCab,Id' }
    Fecha:     { type: date, index: index }
    Importe:   { type: decimal, default: 0 }
    
AgtEntradasCab:
  comment: Cabecera de entradas
  columns:
    Fecha:        { type: date, index: index }
    Documento:    { type: varchar(50), default: null }
    IdAlmacen:    { type: bigint(11), default: 0, index: index, comment: 'datos,Almacenes,Id' }
    IdFirma:      { type: bigint(11), default: 0, index: index, comment: 'datos,Firmas,Id' }
    Recepcionado: { type: boolean, default: 0 }
    
AgtEntradasLineas:
  comment: Lineas de entradas
  columns:
    IdEntrada:   { type: bigint(11), default: 0, index: index, comment: 'datos,EntradasCab,Id' }
    IdFirma:     { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Firmas,Id' }    
    IdFamilia:   { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Familias,Id' }    
    IdArticulo:  { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Articulos,Id' } 
    Descripcion: { type: string(255), notnull: true }
    Unidades:    { type: decimal, default: 0 }
    Precio:      { type: decimal, default: 0 }
    Importe:     { type: decimal, default: 0 }

AgtCondicionesComerciales:
  comment: Condiciones comerciales
  columns:
    IdFirma:     { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Firmas,Id' }    
    IdFamilia:   { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Familias,Id' }    
    IdCondicion: { type: int, notnull: true, default: 0, index: index }    
    Descuento1:  { type: decimal, default: 0 }
    Descuento2:  { type: decimal, default: 0 }
    Comision:    { type: decimal, default: 0 }
    FormaPago:   { type: varchar(50), default: null }

    
# Cabeceras de facturas de firmas
AgtFacturasFirmasCab:
  comment: Cabeceras de facturas de firmas
  columns: 
    Serie:                  { type: char(1), notnull: true, index: index }
    IdFirma:                { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Firmas,Id' }
    IdCliente:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Clientes,Id' }
    IdDirec:                { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,ClientesDEntrega,Id' }
    IdSubagente:            { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Usuarios,Id' }
    SuFactura:              { type: varchar(15), default: null, index: index }   
    Referencia:             { type: varchar(15), default: null }       
    Fecha:                  { type: date, notnull: true, default: '0000-00-00', index: index }
    LiquidacionFirma:       { type: varchar(15), default: null }               
    LiquidacionSubagente:   { type: varchar(15), default: null }               
    ComisionAgente:         { type: decimal, notnull: true, default: 0 }
    ComisionSubagente:      { type: decimal, notnull: true, default: 0 }
    Descuentos:             { type: decimal, notnull: true, default: 0 }    
    DescuentoProntoPago:    { type: decimal, notnull: true, default: 0 }    
    GastosEnvio:            { type: decimal, notnull: true, default: 0 }    
    BaseImponible1:         { type: decimal, notnull: true, default: 0 }    
    Iva1:                   { type: decimal, notnull: true, default: 0 }    
    CuotaIva1:              { type: decimal, notnull: true, default: 0 }    
    Recargo1:               { type: decimal, notnull: true, default: 0 }    
    CuotaRecargo1:          { type: decimal, notnull: true, default: 0 }    
    BaseImponible2:         { type: decimal, notnull: true, default: 0 }    
    Iva2:                   { type: decimal, notnull: true, default: 0 }    
    CuotaIva2:              { type: decimal, notnull: true, default: 0 }    
    Recargo2:               { type: decimal, notnull: true, default: 0 }    
    CuotaRecargo2:          { type: decimal, notnull: true, default: 0 }    
    BaseImponible3:         { type: decimal, notnull: true, default: 0 }    
    Iva3:                   { type: decimal, notnull: true, default: 0 }    
    CuotaIva3:              { type: decimal, notnull: true, default: 0 }    
    Recargo3:               { type: decimal, notnull: true, default: 0 }    
    CuotaRecargo3:          { type: decimal, notnull: true, default: 0 }    
    TotalBases:             { type: decimal, notnull: true, default: 0 }    
    TotalIva:               { type: decimal, notnull: true, default: 0 }    
    TotalRecargo:           { type: decimal, notnull: true, default: 0 }       
    TotalFactura:           { type: decimal, notnull: true, default: 0 }    
    Incidencias:            { type: text, default: null }        
    IdEstado:               { type: bigint(11), notnull: true, default: 0, index: index, comment: 'Abstract,EstadosPedidos,IDTipo' }
    IdFormaPago:            { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,FormasPago,Id' } 
    DomicilioPago:          { type: varchar(100), default: null }
    IdAgencia:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Agencias,Id' }
    Portes:                 { type: string(40), default: null }
    Liquidar:               { type: boolean, notnull: true, default: 0, index: index, comment: 'Abstract,ValoresSN,IDTipo' }
    Servido:                { type: boolean, notnull: true, default: 0, index: index, comment: 'Abstract,ValoresSN,IDTipo' }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>       
  relations:
    
# Líneas de facturas de firmas
AgtFacturasFirmasLineas:
  comment: Líneas de facturas de firmas
  columns:  
    IdFactura:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,FacturasFirmasCab,Id' }
    IdFirma:           { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Firmas,Id' }
    IdFamilia:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Familias,Id' }
    IdCliente:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Clientes,Id' }
    IdArticulo:        { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Articulos,Id' }
    Descripcion:       { type: varchar(255), notnull: true }
    Unidades:          { type: decimal(2), notnull: true, default: 0 }
    Precio:            { type: decimal(3), notnull: true, default: '0.000' }
    Descuento1:        { type: decimal(2), notnull: true, default: '0.00' }
    Descuento2:        { type: decimal(2), notnull: true, default: '0.00' }
    Descuento3:        { type: decimal(2), notnull: true, default: '0.00' }
    Importe:           { type: decimal(2), notnull: true, default: '0.00' }
    Iva:               { type: decimal(2), notnull: true, default: '0.00' }
    ComisionAgente:    { type: decimal(2), notnull: true, default: '0.00' }     
    ComisionSubagente: { type: decimal(2), notnull: true, default: '0.00' }  
    IdPedido:          { type: bigint(11), default: 0, index: index, comment: 'db,PedidosCab,Id' }   
    IdLineaPedido:     { type: bigint(11), default: 0, index: index, comment: 'db,PedidosLineas,Id' }     
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>       
  relations:
      
# Firmas Representadas
AgtFirmas:
  comment: Firmas
  columns:    
    RazonSocial:         { type: varchar(50), notnull: true, index: index }
    NombreComercial:     { type: varchar(50), default: null, index: index }
    Cif:                 { type: varchar(15), notnull: true, index: index }
    Direccion:           { type: varchar(50), default: null }
    IDPais:              { type: bigint(11), notnull: true, default: 68, index: index, comment: 'datos,Paises,Id' }
    IdProvincia:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Provincias,Id' }
    IdPoblacion:         { type: bigint(11), notnull: true, default: 0, index: index, comment: 'datos,Municipios,Id' }
    CodigoPostal:        { type: varchar(10), default: null }
    ApdoCorreos:         { type: varchar(10), default: null }
    Telefono:            { type: varchar(30), default: null }
    Fax:                 { type: varchar(30), default: null }    
    Movil:               { type: varchar(30), default: null }    
    EMail:               { type: varchar(50), default: null }    
    Web:                 { type: varchar(50), default: null } 
    Gerente:             { type: varchar(100), default: null }
    DirectorComercial:   { type: varchar(100), default: null }
    EmailGerente:        { type: varchar(100), default: null }
    EmailPedidos:        { type: varchar(100), default: null }
    EmailSoporteTecnico: { type: varchar(100), default: null }
    CContable:           { type: varchar(10), default: null } 
    Banco:               { type: varchar(50), default: null }
    DireccionBanco:      { type: varchar(50), default: null }        
    Iban:                { type: varchar(34), default: null }
    Bic:                 { type: varchar(11), default: null }
    Mandato:             { type: varchar(35), default: null }
    FechaMandato:        { type: date, notnull: true, default: '0000-00-00' }
    PlazoEntrega:        { type: int(4), default: null }
    IdAgencia:           { type: bigint(11), deafult: 0, index: index, comment: 'datos,Agencias,Id' }
    SinPortes:           { type: decimal, default: 0 }
    Vigente:             { type: boolean, notnull: true, default: 1, index: index, comment: 'datos,ValoresSN,IDTipo' }  
    AvisoPedidos:        { type: text, default: null }    
    AvisoFacturas:       { type: text, default: null }    
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>    
  relations:
      
# Firmas Contactos
AgtFirmasContactos:
  comment: Contactos de firmas
  columns:   
    IdFirma:  { type: bigint(11), notnull: true, index: index, comment: 'datos,Firmas,Id' }
    Cargo:      { type: varchar(30), notnull: false }
    Nombre:     { type: varchar(50), notnull: true }
    Telefono:   { type: varchar(50), default: null }
    Fax:        { type: varchar(50), default: null } 
    Movil:      { type: varchar(50), default: null }   
    EMail:      { type: varchar(50), default: null } 
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations: 
      
# RELACIONES N A M ENTRE ENTIDADES
AgtRelaciones:
  comment: Relaciones entre entidades
  columns:
    EntidadOrigen: { type: string(50), notnull: true, index: index }
    IdEntidadOrigen: { type: string(50), notnull: true, default: 0 }
    EntidadDestino: { type: string(50), notnull: true, index: index }
    IdEntidadDestino: { type: string(50), notnull: true, default: 0 }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>
  relations:

# Contadores
AgtContadores:
  comment: Contadores de facturas, etc
  columns:  
    IdTipo:      { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,TiposContadores,IDTipo' }
    Serie:       { type: char(2), notnull: true }
    Contador:    { type: int(8), notnull: true, default: 0 }
    Predefinido: { type: tinyint(1), notnull: true, default: 0, index: index, comment: 'abstract,ValoresSN,IDTipo' }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>     
  relations:
  index:
    Contador: { type: unique, columns: 'IDTipo,Serie'}
    
# Cabeceras de Pedidos
AgtPedidosCab:
  comment: Cabeceras de pedidos
  columns:   
    IdFirma:                { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Firmas,Id' }
    IdCliente:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Clientes,Id' }
    IdDirec:                { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,ClientesDEntrega,Id' }
    IdAlmacen:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Almacenes,Id' }
    IdAgente:               { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Usuarios,Id' }
    SuPedido:               { type: varchar(15), default: null, index: index }   
    Referencia:             { type: varchar(15), default: null }       
    Fecha:                  { type: date, notnull: true, default: '0000-00-00', index: index }
    FechaEntrega:           { type: date, notnull: true, default: '0000-00-00', index: index }            
    ComisionAgente:         { type: decimal, notnull: true, default: 0 }
    ComisionSubagente:      { type: decimal, notnull: true, default: 0 }
    Descuentos:             { type: decimal, notnull: true, default: 0 }    
    DescuentoProntoPago:    { type: decimal, notnull: true, default: 0 }    
    BaseImponible1:         { type: decimal, notnull: true, default: 0 }    
    Iva1:                   { type: decimal, notnull: true, default: 0 }    
    CuotaIva1:              { type: decimal, notnull: true, default: 0 }    
    Recargo1:               { type: decimal, notnull: true, default: 0 }    
    CuotaRecargo1:          { type: decimal, notnull: true, default: 0 }    
    BaseImponible2:         { type: decimal, notnull: true, default: 0 }    
    Iva2:                   { type: decimal, notnull: true, default: 0 }    
    CuotaIva2:              { type: decimal, notnull: true, default: 0 }    
    Recargo2:               { type: decimal, notnull: true, default: 0 }    
    CuotaRecargo2:          { type: decimal, notnull: true, default: 0 }    
    BaseImponible3:         { type: decimal, notnull: true, default: 0 }    
    Iva3:                   { type: decimal, notnull: true, default: 0 }    
    CuotaIva3:              { type: decimal, notnull: true, default: 0 }    
    Recargo3:               { type: decimal, notnull: true, default: 0 }    
    CuotaRecargo3:          { type: decimal, notnull: true, default: 0 }    
    TotalBases:             { type: decimal, notnull: true, default: 0 }    
    TotalIva:               { type: decimal, notnull: true, default: 0 }    
    TotalRecargo:           { type: decimal, notnull: true, default: 0 }       
    TotalPedido:            { type: decimal, notnull: true, default: 0 }    
    Incidencias:            { type: text, default: null }        
    IdEstado:               { type: bigint(11), notnull: true, default: 0, index: index, comment: 'Abstract,EstadosPedidos,IDTipo' }
    IdFormaPago:            { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,FormasPago,Id' }    
    IdAgencia:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Agencias,Id' }
    Portes:                 { type: string(40), default: null }
    Imprimir:               { type: boolean, notnull: true, default: 0, index: index, comment: 'Abstract,ValoresSN,IDTipo' }
    Servido:                { type: boolean, notnull: true, default: 0, index: index, comment: 'Abstract,ValoresSN,IDTipo' }
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>       
  relations:
    
# Líneas de pedidos
AgtPedidosLineas:
  comment: Líneas de pedidos
  columns:  
    IdPedido:             { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,PedidosCab,Id' }
    IdFirma:              { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Firmas,Id' }
    IdFamilia:            { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Familias,Id' }
    IdCliente:            { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Clientes,Id' }
    IdArticulo:           { type: bigint(11), notnull: true, default: 0, index: index, comment: 'db,Articulos,Id' }
    Descripcion:          { type: varchar(255), notnull: true }
    Unidades:             { type: decimal(2), notnull: true, default: 0 }
    Precio:               { type: decimal(3), notnull: true, default: '0.000' }
    Descuento1:           { type: decimal(2), notnull: true, default: '0.00' }
    Descuento2:           { type: decimal(2), notnull: true, default: '0.00' }
    Descuento3:           { type: decimal(2), notnull: true, default: '0.00' }
    Importe:              { type: decimal(2), notnull: true, default: '0.00' }
    Iva:                  { type: decimal(2), notnull: true, default: '0.00' }
    ComisionAgente:       { type: decimal(2), notnull: true, default: '0.00' }     
    ComisionSubagente:    { type: decimal(2), notnull: true, default: '0.00' }  
    UnidadesPtesFacturar: { type: decimal(2), notnull: true, default: '0.00' }   
    Facturar:             { type: boolean, notnull: true, default: 0, index: index, comment: 'Abstract,ValoresSN,IDTipo' }    
    <?php include '../../entities/schema/schemaColumnsCommon.yml'; echoln('');?>       
  relations: